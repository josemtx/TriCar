# Puerto del servidor (en producción lo asigna la plataforma vía PORT)
PORT=5000

# Cadena de conexión a MongoDB (MongoDB Atlas free tier).
# Sustituye por tu propia cadena (NO subas credenciales reales al repo).
MONGODB_URI=tu_cadena_de_conexion_aqui
